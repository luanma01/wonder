
开发过程

	团队规模与组成

	团队开发的配合
	

协议 http/tcp
	
	tcp 协议中的三次握手，四次挥手
		
		客户端（主动打开）	服务器端（被动打开）
			
			SYN=1,seq=x --->		#第一次握手：客户端通过调用connect发起主动打开

			<--- SYN=1,ACK=1,		#第二次握手：服务器收到数据报后，从SYN位为1就知道这是一个建立连接的请求。
			     seq=y,ack=x+1

			ACK=1,seq=x, --->		#第三次握手：客户端收到服务器端的报文段后，要对服务器端中的SYN进行确认
			ack=x+1
		
		客户端（主动关闭）	服务器端（被动关闭）

			FIN=1,seq=u --->		#第一次挥手：假设客户端执行主动关闭，那么它会向服务器端发出释放连接的报文段

			<--- ACK=1,seq=v,		#第二次挥手：服务器端收到客户端的释放连接的报文段后会执行被动关闭，它要对客户端的数据报进行确认
			     ack=u+1

			<--- FIN=1,ACK=1,		#第三次挥手：服务器端已经没有要发送给客户端的数据，那么服务器端也会调用close关闭套接字，这样服务器端也会发送一个FIN的TCP报文段
			     seq=w,ack=u+1

			ACK=1,seq=u+1, --->		第四次挥手：客户端接受到这个最终的FIN的释放连接报文段后必须对报文段进行确认
			ack=w+1

JS 基础、ES6

	基础
		JS 数据类型：

		JS 内置对象11种：

		API
			String		
				toUppercase()
				toLowCase()
				...

			Array		
				slice()
				push()
				...

Vue/vue-router/vueX

	vue 特点
	
	vue 常用指令
		
		v-if	v-show	v-bind	v-on

	...